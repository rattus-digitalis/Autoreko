#ifndef SCAN_H
#define SCAN_H


int is_service_accessible(const char *input);
int perform_nmap_scan(const char *target, int intensity);
int perform_nmap_scan(const char *target, int intensity);

#endif
